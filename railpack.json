{
  "$schema": "https://schema.railpack.com",
  "packages": {
    "node": "22"
  },
  "steps": {
    "build": {
      "inputs": [{ "step": "install" }],
      "commands": ["npx turbo run build --filter=api"]
    }
  },
  "deploy": {
    "startCommand": "npm run start:railway --workspace=api"
  }
}
