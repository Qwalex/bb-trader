/** UserSetting + env chain: не в `CABINET_SCOPED` — одно значение на аккаунт. */
export const LEVERAGE_CALCULATOR_PRESET_KEY = 'LEVERAGE_CALCULATOR_PRESET';

export const PRESET_JSON_MAX_LEN = 4000;
